import SearchClient from './SearchClient';

export const metadata = {
  title: 'Search Results — SmartRail',
  description: 'View AI-ranked journey results with smart recommendations, delay risk analysis, and multi-criteria scoring.',
};

export default function SearchPage() {
  return <SearchClient />;
}
